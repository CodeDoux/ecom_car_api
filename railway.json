{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
     "preDeployCommand": "php artisan migrate --force && php artisan db:seed --class=SettingSeeder --force && php artisan db:seed --class=UserSeeder --force",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}